project_id = "xlfwzqpixlrnntzqhvcm"

[functions.client-portal]
verify_jwt = false

[functions.clickup-webhook]
verify_jwt = false

[functions.send-report-email]
verify_jwt = false

[functions.generate-proposal]
verify_jwt = false

[functions.elevenlabs-transcribe]
verify_jwt = false

[functions.polish-text]
verify_jwt = false

[functions.extract-payapp-lines]
verify_jwt = false

[functions.lien-countersign]
verify_jwt = false

# Change-order counter-sign — public, token-gated (matches lien-countersign)
[functions.co-countersign]
verify_jwt = false

# Authored correspondence document counter-sign — public, token-gated
[functions.document-countersign]
verify_jwt = false

# Public action-item card for CRM / external assignees — token-gated
[functions.action-item-public]
verify_jwt = false

# Proposal acceptance links are public capability URLs; the UUID token scopes
# access to one proposal and the functions validate it before returning data.
[functions.proposal-countersign]
verify_jwt = false

[functions.financial-proposal-countersign]
verify_jwt = false

# Daily report action-item notification — authenticated admin invokes; emails the
# report's submitter a digest of open action items via Resend.
[functions.daily-report-action-notify]
verify_jwt = true

# Punch list response — public, token-gated (sub responds with no login)
[functions.punch-respond]
verify_jwt = false

# AI punch list drafting — public so dictation/typing works without auth friction
[functions.draft-punch-items]
verify_jwt = false

# AI change-order drafting — authenticated GC invokes it; explicit for clarity
[functions.draft-change-order]
verify_jwt = true

# AI financial-proposal drafting — authenticated consultant invokes it
[functions.draft-financial-proposal]
verify_jwt = true

[functions.send-invitation]
verify_jwt = false

# Public only at the gateway because the signed, single-use invitation token is
# the credential. The function validates origin, expiry, revocation and email,
# then creates one confirmed Auth user through the service role.
[functions.accept-workspace-invitation]
verify_jwt = false

# Authenticated workspace-owner/admin user activation controls. The function
# verifies asymmetric Supabase JWTs with auth.getUser() and then enforces the
# tenant hierarchy through can_administer_workspace_user().
[functions.manage-workspace-user]
verify_jwt = false

[functions.send-email]
verify_jwt = false

[functions.process-articulate-course]
verify_jwt = false

[functions.voice-agent-token]
verify_jwt = false

[functions.voice-agent-tools]
verify_jwt = false

[functions.voice-agent-webhook]
verify_jwt = false

[functions.serve-course-content]
verify_jwt = false

[functions.extract-transcript-issues]
verify_jwt = false

[functions.generate-progress-report]
verify_jwt = false

[functions.send-push-notification]
verify_jwt = false

[functions.regulatory-case-review]
verify_jwt = false

# ───── A6 · Stripe Billing & Plans ─────
# Webhook must NOT verify JWT — Stripe signs the body with STRIPE_WEBHOOK_SECRET
[functions.stripe-webhook]
verify_jwt = false

[functions.create-checkout]
verify_jwt = true

[functions.stripe-billing-portal]
verify_jwt = true

# ───── A7 · SSO / SCIM ─────
# SAML ACS is the IdP callback — no JWT, signature-verified via IdP certificate
[functions.saml-acs]
verify_jwt = false

# SCIM endpoints use their own Bearer token (hashed in tenant_scim_tokens)
[functions.scim-users]
verify_jwt = false

[functions.scim-groups]
verify_jwt = false

# ───── Phase 4 · E3 / F1-F2 / F3 ─────
# OAuth token endpoint — public, validates client_id/secret inside
[functions.oauth-token]
verify_jwt = false

# Public REST API — validates its own Bearer token via api_tokens table
[functions.api-v1]
verify_jwt = false

# API client minting validates the caller with auth.getUser() inside the
# function. Disable the legacy gateway JWT check so asymmetric user JWTs from
# the current Supabase Auth stack reach that explicit authorization path.
[functions.api-key-mint]
verify_jwt = false

# Webhook dispatcher — invoked by Supabase cron only
[functions.webhook-dispatch]
verify_jwt = false

# Report runner — invoked via supabase.functions.invoke (needs JWT for RLS on reports)
[functions.run-report]
verify_jwt = true

# Portal invitation acceptance — public (token is the auth)
[functions.accept-portal-invitation]
verify_jwt = false

# ───── Phase 2 · B4 / C4 post-processors ─────
# Photo-process — generates thumbnails + EXIF patch; callable by authenticated client
[functions.photo-process]
verify_jwt = true

# Daily weather — fills daily_reports forecast fields from Open-Meteo
[functions.daily-weather]
verify_jwt = true

# ───── Phase 2 · E1 schedule importers ─────
# Primavera P6 XER parser — authenticated; RLS keeps writes tenant-scoped.
[functions.parse-p6-xer]
verify_jwt = true

# Microsoft Project XML parser — authenticated; same RLS contract as P6.
[functions.parse-msp-xml]
verify_jwt = true

# Public capture link — anonymous field uploads validated by token, service-role insert.
[functions.gallery-capture]
verify_jwt = false

# Public bid submission — anonymous subs submit a bid via token, service-role insert.
[functions.bid-submit]
verify_jwt = false

# Nightly risk radar — invoked by pg_cron, authorized by a shared secret (not JWT).
[functions.risk-radar-nightly]
verify_jwt = false

# Public client-portal data — slug-scoped, service-role, client-safe fields only.
[functions.portal-data]
verify_jwt = true

[functions.portal-action]
verify_jwt = false

[functions.vendor-submit]
verify_jwt = false

# Public Gmail OAuth callback — Google redirects here with ?code&state and no
# Supabase JWT; authenticity is enforced by the signed `state`.
[functions.gmail-oauth-callback]
verify_jwt = false

# Authenticated project texting through a server-only Twilio connection.
[functions.sms]
verify_jwt = true

# Twilio signs delivery and inbound-message webhooks itself.
[functions.sms-webhook]
verify_jwt = false

# Authenticated project-task bridge. Trello credentials remain server-side and
# every push re-checks project access through the caller's JWT/RLS.
[functions.trello]
verify_jwt = true

# Authenticated fan-out for assignment/comment/status mobile push notices.
[functions.project-task-notify]
verify_jwt = true
