import R4LandingPage from './R4LandingPage';

/**
 * Public marketing entry point. The operational application remains unchanged;
 * this route only presents the R4-focused APAS Project Controls experience.
 */
export default function LandingPage() {
  return <R4LandingPage />;
}
