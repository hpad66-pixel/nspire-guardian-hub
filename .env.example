# ─────────────────────────────────────────────────────────────
# Supabase — standalone project (migrated off Lovable Cloud)
# Copy this file to .env.local and fill in real values.
# In GitHub, these same names must exist as repo secrets for CI + Deploy.
# ─────────────────────────────────────────────────────────────

# Frontend (baked into Vite build — public)
VITE_SUPABASE_URL=https://xlfwzqpixlrnntzqhvcm.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<anon-public-key>
VITE_SUPABASE_PROJECT_ID=xlfwzqpixlrnntzqhvcm

# Supabase CLI (deploy pipeline only — never commit real values)
# Needed by .github/workflows/deploy.yml for `supabase link` + `supabase db push`.
SUPABASE_ACCESS_TOKEN=<personal-access-token-from-supabase.com/dashboard/account/tokens>
SUPABASE_DB_PASSWORD=<database-password-for-project>

# Optional — set only if edge functions or seed scripts need admin access
# SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# Proj OS remote MCP (Cloudflare Pages runtime — never expose in VITE_* vars)
# Create a scoped API client in Settings → API Clients, then store its once-only
# client secret in the deployment environment. Use a separate random shared
# secret for Hermes to authenticate to https://<proj-os-host>/mcp.
# PROJ_OS_MCP_SHARED_SECRET=<random-long-secret>
# PROJ_OS_CLIENT_ID=<api-client-id>
# PROJ_OS_CLIENT_SECRET=<api-client-secret>
# PROJ_OS_SUPABASE_FUNCTIONS_URL=https://xlfwzqpixlrnntzqhvcm.supabase.co/functions/v1
# PROJ_OS_MCP_ALLOWED_ORIGINS=https://projos.ai
# Legacy only: leave disabled after the scoped Agent Gateway is configured.
# PROJ_OS_LEGACY_MCP_ENABLED=false

# Scoped Agent Gateway (Supabase Edge Functions)
# Public key can be copied to Hermes; the private key must remain in Proj OS.
# Generate a P-256 signing key outside the repository and provide JWK JSON.
# AGENT_SESSION_ISSUER=https://projos.ai
# AGENT_SESSION_AUDIENCE=proj-os-agent-runtime
# AGENT_SESSION_KEY_ID=<non-secret-key-id>
# AGENT_SESSION_PRIVATE_JWK=<server-only-p-256-private-jwk-json>
# AGENT_SESSION_PUBLIC_JWK=<p-256-public-jwk-json>
# AGENT_GATEWAY_ALLOWED_ORIGINS=http://localhost:5173,https://projos.ai

# Production Agent panel — disabled unless the complete read-only path is configured.
# VITE_AGENT_FOUNDATION_ENABLED=false
# VITE_AGENT_RUNTIME_URL=https://agent-runtime.your-domain.example
# Development-only synthetic Proj OS gateway used by /agent-foundation-preview.
# Never set this in production; authenticated production sessions use agent-session.
# VITE_AGENT_LOCAL_SESSION_URL=http://127.0.0.1:8788/v1/sessions

# APAS CRM business-card scan — keep off until the migration, Edge Function,
# server-side secrets, and per-user/project pilot entitlements are in place.
# VITE_CRM_CARD_SCAN_ENABLED=false

# E2E tests (Playwright)
E2E_BASE_URL=http://localhost:5173
