/**
 * Glorieta Gardens — WASD bill backup
 *
 * Original Miami-Dade WASD PDF binaries were never committed. This folder is
 * the checked-in reconstruction of the ten statements OCR'd from the
 * June/July 2026 cycle (PR #382). Amounts, meters, and readings match
 * `src/lib/water-intel/glorietaArchive.ts` and the overlay migration.
 *
 * HTML quick-views are served from `/water-bills/*.html` on the live site.
 */

# Glorieta WASD bill archive

Ten OCR'd Miami-Dade WASD statements from the June/July 2026 cycle. Building 8 (acct `2745714336`) amount due is **$122,667.65**.

Open `glorieta-wasd-june-2026.json` for the machine-readable backup. Live statement views: `/water-bills/`.
